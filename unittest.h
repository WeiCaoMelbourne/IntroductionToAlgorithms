#ifndef CW_UNITTEST_H
#define CW_UNITTEST_H

#include <stdlib.h>

void print_array(int* array, size_t len);
void copy_array(int* array, size_t len, int* target);
int compare_arrays(int* array, size_t len, int* target);

#endif
